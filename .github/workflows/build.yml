name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  linux:
    name: Linux (GCC 14)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-14 ninja-build \
            libx11-dev libxext-dev libxrandr-dev libxi-dev libxcursor-dev \
            libwayland-dev libxkbcommon-dev libdecor-0-dev \
            libgl-dev libegl-dev libpulse-dev libasound2-dev \
            libdbus-1-dev libudev-dev
      - name: Configure
        run: cmake -B build -G Ninja -DCMAKE_C_COMPILER=gcc-14
                   -DCMAKE_BUILD_TYPE=RelWithDebInfo -DGAVAGA_WERROR=ON
      - name: Build
        run: cmake --build build -j
      - name: Smoke test
        env:
          SDL_VIDEODRIVER: dummy
          SDL_RENDER_DRIVER: software
        run: ./build/gavaga --play --debug --shot smoke.bmp --shot-at 5000
      - uses: actions/upload-artifact@v4
        with:
          name: linux-frame
          path: smoke.bmp

  windows:
    name: Windows x64 (MSVC)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      # No -G: the runner image changes which Visual Studio it ships, so let
      # CMake pick the newest installed one rather than naming a version.
      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo
      - name: Build
        run: cmake --build build --config RelWithDebInfo --parallel
      - uses: actions/upload-artifact@v4
        with:
          name: windows-x64
          path: build/**/gavaga.exe

  macos:
    name: macOS (Apple Silicon)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo
      - name: Build
        run: cmake --build build -j3
      - uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: build/gavaga
